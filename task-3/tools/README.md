# tools/

Build & patch tooling for the n8n workflow that backs the bot. The final, importable workflow is `../workflow.json` — these scripts document how it was assembled iteratively.

## Why patches and not raw workflow edits

n8n's editor UI gets slow once a workflow exceeds ~40 nodes (this one ended at 62). Re-importing JSON via the UI also clobbers credentials. So the iteration loop was:

1. Dump current `nodes` + `connections` JSON from `workflow_entity` / `workflow_history` (Postgres).
2. Run a Python patch script that mutates the dump in memory (add nodes, rewire connections, swap node types, replace `jsCode` / `query` / `jsonBody`).
3. Write the result back to both tables.
4. `docker restart task3-n8n` to pick up changes.

This kept iteration time at ~15 seconds end-to-end vs. ~2 minutes per re-import via UI.

## Order of application (chronological)

| # | Script | What it did | Why it was needed |
|---|--------|-------------|-------------------|
| 01 | `01-megabuild-utf8.py` | Initial 30+ node build with UTF-8 safe encoding pipeline | Windows Python defaults to cp1251 — emoji and Cyrillic in JS code nodes were getting double-encoded mojibake (`рџ”»`). Solution: explicit `encoding='utf-8'` on file open, `PYTHONIOENCODING=utf-8` for stdout, run patch entirely in Linux container via `docker exec` to avoid Windows codec layer. |
| 02 | `02-quiz-sql-inline.py` | Rewrote all SQL queries to use inline `{{ ... }}` expressions instead of `queryReplacement` | n8n Postgres node `queryReplacement` does *text* substitution, not parameter binding. With `JSON.stringify` wrapping, literal quotes ended up as part of values, blowing up CHECK constraints (`'intermediate'` instead of `intermediate`). |
| 03 | `03-quiz-chain-rewire.py` | Injected `pick: build examiner body` Code node between `pg load material` and `Examiner (Haiku)` | HTTP Request `jsonBody` parser choked at byte 2009 on the inline `{{ ... }}` JSON. Building the body string in JS upstream avoided n8n's expression interpolation entirely. |
| 04 | `04-sendpoll-quiz-mode.py` | Added 10 nodes implementing Telegram `sendPoll` quiz mode (vs. inline-keyboard buttons) | Telegram's native quiz UI is more polished; gives instant correctness feedback, native explanations, doesn't bloat chat with N callback messages. |
| 05 | `05-finalize-dual-source.py` | `ans: pg finalize quiz` & `ans: pg load all answers` SQL changed to read `quiz_id` from `poll_ans: validate` OR `ans: validate` via `.isExecuted` ternary | Two converging paths into finalize (inline-kb vs poll). The path-not-taken node hadn't executed, so its reference threw `ExpressionError: Node 'X' hasn't been executed`. |
| 06 | `06-format-dual-source.py` | Same dual-source pattern in `ans: format results` jsCode | Code node was reading `$('ans: validate').item.json.chat_id` — broke for poll-answer path. |
| 07 | `07-wow-features.py` | Added 10 nodes: `setMessageReaction` calls (🤔 / 🎓 on `/learn`), Mini App webhook → Postgres → HTML dashboard, `inline_query` handler, `web_app` button on `/stats`, `message_effect_id` on score reply | Differentiator features. Bot API 7.0 features (reactions, effects) added Mar 2024 — few entries will use them. Mini App via `web_app` inline button + n8n Webhook+RespondToWebhook nodes serving HTML; Chart.js for charts. |
| 08 | `08-miniapp-v2-redesign.py` | Mini App HTML v2: glassmorphism, animated gradient hero, count-up KPIs, achievement strip (7 badges), 28-day activity heatmap, score-trend chart with gradient fill, library w/ best-score bars, Telegram WebApp theme & MainButton integration | First Mini App was functional but generic. Rebuilt with care for sub-second load + native-feeling animations. |
| 09 | `09-effect-html-mode.py` | Convert `ans: format results` text from Markdown to HTML formatting | **Bug discovered via diff testing**: `parse_mode: "Markdown"` (legacy) **silently drops** `message_effect_id` — Telegram delivers the message without the effect and without an error. `parse_mode: "HTML"` echoes `effect_id` back in the response and the animation plays. Reproduced with curl. |
| 10 | `10-ans-send-http-request.py` | Replace n8n Telegram node v1.2 (`ans: send results`) with HTTP Request | The Telegram node hardcodes `parse_mode: Markdown` and discards unknown fields like `message_effect_id`. Also threw `Bad Request: message is too long` once HTML angle-brackets entered the text (suspect double-encoding). HTTP Request gives full control of the JSON body. |
| 11 | `11-split-result-msg.py` | Split final score into TWO messages: short headline with effect + long breakdown without | Telegram message effects only animate reliably on **short** messages (likely a client heuristic — long messages get static effect indicator only). Headline ≈ 80 chars carries the effect, breakdown carries the 5-question detail. |
| 12 | `12-wait-for-effect-anim.py` | Inserted n8n `Wait` node (3.5 sec) between headline and breakdown | Without the gap the breakdown shoves in instantly and pushes the headline out of view *before the animation finishes*. 3.5s lets the fullscreen effect complete. |
| 13 | `13-localization.py` | Full RU/EN localization: `app.user_state.lang` column + `/lang` command with inline-kb picker + `lang_cb:*` callback handler + five `lang: pg load *` Postgres lookup nodes seeded into /learn, /quiz, /stats, pick, ans chains + i18n string dict in all UX-emitting Code nodes + lang directive appended to Teacher & Examiner system prompts | Russian-speaking audience expects native bot. Implementation: lookup node pulls `lang` from `user_state` (default `'en'`); each Code node reads via `$('lang: pg load X').item.json.lang` and selects from local `T = { en: {...}, ru: {...} }` dict. LLM prompts get an appended directive forcing output language. |
| 14 | `14-lang-load-input-refs.py` | Rewired `$input.first().json` → `$('source_node').item.json` in 5 Code nodes downstream of lang loaders | **Bug from patch 13**: inserting a Postgres lookup node mid-chain replaces the downstream node's `$input` data. Format-summary's `$input.first().json` was now `{lang:'en'}` instead of the material row → `teacher_body` came out `undefined` → Anthropic HTTP node 500'd with "JSON Body field is not valid JSON". Fix: explicit `$('upstream_named_node').item.json` references that don't shift. |
| 15 | `15-i18n-url-placeholder.py` | Replaced literal `<url>` with `[URL]` in all i18n strings across 6 Code nodes | **Bug from patch 13**: localized footer text contained `Open /stats or /learn <url>`. Sent with `parse_mode: HTML`, Telegram's parser saw `<url>` as opening tag and rejected the message: `Bad Request: can't parse entities: Unsupported start tag "url" at byte offset 5037`. `[URL]` is safe in both Markdown and HTML modes. |
| 16 | `16-ack-timing-warnings.py` | Added `/learn: build ack text` + `pick: build ack text` Code nodes producing localized "1–3 minutes, grab a coffee ☕" messages, wired in parallel with the LLM-call chain | Users were confused why nothing happened for 30–60 seconds after `/learn` or quiz pick. The pre-existing /learn ack said "30–60 seconds" which under-promised; the pick callback had no ack at all and looked frozen. Now both show explicit timing + reassurance + Telegram language_code / stored `lang` driving the text. |
| 17 | `17-start-mojibake-and-breakdown-chunking.py` | (a) Rewrote `Reply /start1` text — DB had double-encoded UTF-8 (`рџ‘‹` instead of `👋`) from the megabuild stdin pipe; converted node from Telegram type to HTTP Request with a Code-node builder using clean UTF-8 source. (b) Quiz-result breakdown now chunks into ≤ 3500-char messages with `Часть N/M` labels if the long-text version exceeds Telegram's 4096-char limit (common with Russian content + 5 explanations + Cyrillic 2-bytes-per-char). | `Bad Request: message is too long` from Telegram on Russian quizzes. /start mojibake reported by a fresh tester. |
| 18 | `18-ack-runs-first.py` | Reordered `lang: pg load pick` `main[0]` to put `pick: build ack text` first | Theory: n8n executes multi-target branches in connection-list order. **Theory was wrong** — see patch 19. Kept as historical artifact. |
| 19 | `19-ack-sequential-not-parallel.py` | Restructured topology: ack node moved from a *parallel sibling* of `pick: build examiner body` to a *sequential link* between `pg load material` and `lang: pg load pick`. | **n8n actually does depth-first traversal of multi-target branches** — the full subtree of one target completes before the next target starts. Putting ack parallel to a 25-second Examiner call meant ack arrived 25s after the question, not before. Sequential placement fixes timing. |
| 20 | `20-pick-ack-uses-stored-lang.py` | Inserted `lang: pg load pick` BEFORE ack in the new sequential chain so ack honours the user's stored `/lang` preference, not Telegram client `from.language_code` | Patch 19's ack used `tg.from.language_code` as a one-shot fallback (instant, no DB) — but for users who switched language via `/lang`, the ack came in their **Telegram client language** instead of their **chosen bot language**. Added an extra ~15ms DB roundtrip in exchange for correct language. |
| 21 | `21-quiz-pool-architecture.py` | **Major feature**: pre-generated quiz pool (`app.quiz_pool` table). New webhook `/pool-refill` triggers a self-firing Sonnet 4.5 generation chain that maintains 3 quizzes per (material, lang). Pick callback splits into fast-path (atomic `DELETE...FOR UPDATE SKIP LOCKED RETURNING` from pool → instant Q1) and slow-path (existing Examiner call when pool empty). Examiner model upgraded Haiku 4.5 → Sonnet 4.5 (better question quality; speed acceptable since pool runs in background). | User feedback: Haiku quiz quality was inconsistent and 30–60s wait felt long. Pre-generation absorbs the wait into the gap between `/learn` summary delivery and the user tapping "Take quiz now" — by then 1–3 pool entries are usually ready. Concurrency-safe via `SKIP LOCKED` (two users on same material claim different pool entries). |
| 22 | `22-pool-backfill-and-uniqueness.py` | Stronger uniqueness directive (5 explicit rules + self-check requirement + "at most 1 overlap" escape hatch) + new `pool: similarity check` Code node that computes Jaccard-bigram similarity of each new question vs all priors. Logs warnings when similarity > 0.55 (visible in `docker logs task3-n8n`) without blocking generation. Companion `tools/warmup-pool.sh` script fires refill for every existing (material, lang) combo. | Pool entries 1–3 verified diverse on first material (3 distinct angles: monolith tradeoff, cross-service data, shared lib coupling), but as pool depth grows the LLM tends to drift toward repetition. The similarity check is observability, not enforcement — hard rejection risks infinite-loop regen when material is genuinely exhausted. Backfill script onboards materials added before the pool feature shipped. |
| 23 | `23-refill-fires-once-per-quiz.py` | Moved `pick: trigger pool refill` from `pg INSERT quiz_polls` (which fires once per poll, Q1-Q5 = 5×) to `pg INSERT quiz` (fires once per quiz creation). | **Bug from patch 21**: refill fired 5 times per quiz, and 4 of those firings threw `ExpressionError: Node 'pick: pg load material' hasn't been executed` because the poll_ans chain (Q2-Q5) doesn't run `pg load material`. Fix: anchor refill at the per-quiz node, not the per-poll node. |
| 24 | `24-localization-final-leaks.py` | Closed two remaining i18n leaks: (a) `/start: pg seed lang` Postgres step inserts `user_state.lang` based on Telegram client `language_code` on the very first `/start` (idempotent via `ON CONFLICT DO NOTHING`) — first-time RU users now get RU summaries on their first `/learn` without manually `/lang`-ing first. (b) `/quiz: build topic keyboard` localized via new `lang: pg load /quiz` lookup — "Pick a topic to quiz on:" now reads "Выбери материал для квиза:" for RU users. | Discovered via cross-account smoke test on mom's phone (RU Telegram client): welcome was RU but first `/learn` summary came in EN because `user_state` had no row yet (default 'en'). After manual `/lang ru`, quiz content was RU but the topic picker stayed EN — a stranded hard-coded string. Both classes fixed. |
| 25 | `25-default-lang-en.py` | Reverted patch 24's client-language auto-detect: `/start: pg seed lang` now always inserts `'en'` regardless of Telegram client language, and `/start: build text` always emits the English welcome (dropped the RU branch). Users who prefer Russian explicitly choose it via `/lang`. | Operator decision after seeing patch 24 in action: auto-detection from Telegram client is too clever — a RU-Telegram user might prefer EN content (English is the lingua franca of senior backend engineering), and EN as universal default is more conservative. Russian still fully supported, just opt-in. |
| 26 | `26-learn-ack-sequential-and-localized.py` | Rewired `/learn: URL valid?` TRUE branch from parallel fan-out `[build ack, jina fetch, react thinking]` to a fully sequential chain: `lang: pg load /learn → build ack → send ack → react thinking → jina fetch → extract title+body`. Also moved `lang: pg load /learn` from after-jina to before-ack so the ack text honours stored `/lang` preference. | **Major UX bug discovered via exec 217/243 inspection**: in EVERY recent /learn exec, only `jina fetch` ran from the parallel fan-out. n8n's IF node (and others) doesn't actually fan-out multi-target connections — it silently picks one. The "🔍 Reading the article..." ack and the 🤔 reaction were both dead code. Users typed /learn URL and saw NO feedback for 10-180 seconds. Sequential chain adds ~900ms latency to summary delivery but guarantees instant visible feedback. |
| 27 | `27-jina-fetch-named-ref.py` | Changed `/learn: jina fetch` URL from `https://r.jina.ai/{{ $json.url }}` to `https://r.jina.ai/{{ $('/learn: parse URL').item.json.url }}` | **Regression caused by patch 26**: in the new sequential chain, `jina fetch` is preceded by `learn: react thinking` (Telegram setMessageReaction HTTP node) instead of `URL valid?`. `$json.url` was reading react thinking's response (the Telegram API result, no `url` field) → URL became `https://r.jina.ai/undefined` → jina returned its landing-page placeholder → `extract title+body` saw < 200 chars → `extraction_failed: true` → Teacher body undefined → 400 from Anthropic. Same lesson as patch 14: positional `$json` references die when the chain shape changes; named `$('NodeName').item.json` refs survive. |
| 28 / 28b | `28-poll-length-limits.py` + `28b-pick-parse-truncation.py` | Three-layered fix for poll option / explanation truncation: (1) Examiner system prompts tightened — options must be ≤ 95 chars, explanation ≤ 195 chars (5-char safety margin under Telegram's 100/200 hard ceilings) with a CRITICAL char-count self-check directive. (2) Runtime truncation in both parse-Examiner-JSON nodes — `truncWithEllipsis(s, max)` defensively shortens any field that slipped through with `…` indicator. (3) Pool reset: `TRUNCATE app.quiz_pool` + `tools/warmup-pool.sh both` to regenerate all entries under the new rules. | **Critical UX bug spotted on RU quiz**: 5/5 poll options showed "..." at the end and the correct-answer reveal sometimes lost its tail mid-sentence. Telegram silently truncates `sendPoll` `option.text` at 100 chars and `explanation` at 200. The old prompt allowed up to 120 chars for options and "2-4 sentences" for explanations — Sonnet 4.5 produced ~120-char options frequently. Existing pool entries from before this patch all suffer the same issue, hence the full pool reset. |
| 29 | `29-pool-truncation-and-poll-race.py` | (A) Patch 28's auto-injection FAILED for `pool: parse Examiner JSON` (different code structure). Manually rewrote with `truncWithEllipsis` in the per-question map. (B) Inserted `Wait(1.5s)` between `poll_ans: parse` and `poll_ans: pg load` to absorb a race where fast-tapping users answer Q1 before workflow's `pg INSERT quiz_polls` commits. | Two failure modes spotted on a fresh test: new pool entries STILL had 130-540 char options because patch 28's injection only landed on pick-side. Quiz silently stopped at Q1-Q4 because pool refill ran before quiz_polls row was committed (poll_ans pg load returned 0 rows). |
| 30 | `30-learn-sql-params-and-restore-sequential.py` | (A) Switched `/learn: pg INSERT material` from text-substitution (`'{{ $json.title.replace(...) }}'`) to PostgreSQL parameter binding via `queryReplacement` with `$1..$6` placeholders. (B) Restored patch 26's sequential `/learn` chain wiring that got lost between patches 26-29 (a later patch's dump preceded a write that reverted it). | Metanit.com F# article content had patterns that broke n8n's expression engine when interpolated into SQL string — Postgres got malformed query with `INSERT INTO` seemingly nested. Param binding bypasses all template-level escaping concerns. |
| 31 | `31-build-insert-sql-in-code.py` | Switched again — this time building the complete INSERT SQL **in JS context** inside `parse Teacher JSON` Code node, with manual single-quote escape + NUL strip. Postgres node now just executes `={{ $json.insert_sql }}` verbatim. | Patch 30's parameter binding worked for text columns but mangled UTF-8 on the JSONB column: Cyrillic summary content caused `invalid input syntax for type json. Token "??" is invalid.` — `??` is PostgreSQL's representation of unrecognised bytes (UTF-8 multi-byte char cut mid-sequence during n8n→postgres parameter encoding). JS-built SQL bypasses both the expression engine AND the parameter binding layer. Same pattern as Teacher/Examiner request body building. |
| 32 | `32-parameterize-ngrok-domain.py` | Replaced hardcoded `seniorprepcoach.ngrok.dev` in 4 workflow nodes (Mini App dashboard URL, pool-refill self-fire URL, pool-refill trigger URLs in /learn and pick) with `{{ $env.NGROK_DOMAIN }}` (or `${$env.NGROK_DOMAIN}` in Code nodes). | Reusability bug: anyone importing `workflow.json` into their own n8n instance would have webhook calls hardcoded to *our* ngrok domain instead of theirs. Not a security risk (pool-refill is no-op on unknown material_ids) but a real reusability blocker. After this patch `workflow.json` is fully portable — every public URL resolves from the deployer's `NGROK_DOMAIN` env var. Older patches (07, 13, 21) still contain the literal domain string as historical build artefacts; the live `workflow.json` is clean. |
| 29 | `29-pool-truncation-and-poll-race.py` + SQL in-place pool truncate | (A) Patch 28's auto-injection FAILED for `pool: parse Examiner JSON` (different code structure). Manually rewrote it with `truncWithEllipsis` in the per-question map — options to 95, explanation to 195, question to 280. (B) Inserted `Wait(1.5s)` node between `poll_ans: parse` and `poll_ans: pg load` to absorb the race condition where fast-tapping users answer Q1 before the workflow's `pg INSERT quiz_polls` row commits. (C) Bonus SQL: `UPDATE app.quiz_pool SET questions = ...truncated...` to fix existing entries in-place instead of waiting for organic regeneration. | **Two failure modes spotted on a fresh test session**: (1) New pool entries STILL had 130-540 char options/explanations because patch 28's truncation only landed on `pick:`-side, not pool-side. (2) Quiz silently stopped at Q1-Q4 — root cause: poll_answer webhook arrived at n8n ~50-200ms after Telegram delivered the poll to a fast user, but workflow's `pg INSERT quiz_polls` (in the parallel branch after `send poll → capture poll_id`) hadn't committed yet. `poll_ans: pg load WHERE poll_id = X` returned empty rows → chain stopped silently → user stuck. The Wait absorbs the race for the worst-case insertion latency. |

## How to re-apply on a fresh n8n

The fastest path is to import `../workflow.json` via the n8n editor UI (Settings → Import from File). All patches are already baked in.

The patch scripts here are kept for **traceability** — they document each design decision and the bug that motivated it. If you ever need to rebuild from a different base, the scripts demonstrate the pattern: dump → mutate JSON → write back → restart.

## Encoding caveat (Windows)

Every script in this folder reads/writes with `encoding='utf-8'` explicitly. Don't run them via `python script.py | python other.py` without `PYTHONIOENCODING=utf-8` set — Windows defaults stdin/stdout to cp1251, which destroys emoji and Cyrillic.
